language: generic
sudo: false
before_install:
  - openssl aes-256-cbc -K $encrypted_7dd107a11548_key -iv $encrypted_7dd107a11548_iv -in signing.key.enc -out signing.key -d
script:
  - bash updrepo.sh $TRAVIS_REPO_SLUG
before_deploy:
  - cp -a repo $TRAVIS_TAG
deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  file_glob: true
  file: ${TRAVIS_TAG}/*.{db,files,gz,xz,sig}
  overwrite: true
  skip_cleanup: true
  on:
    all_branches: true
